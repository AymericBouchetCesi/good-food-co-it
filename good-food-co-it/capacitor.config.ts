import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'good-food-co-it',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
